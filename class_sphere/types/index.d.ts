interface CardProps {
  title: string;
  value: string | number;
  icon: string;
}

interface user {
    name?: string;
    
}

interface PageProps {
    params: {
      admin: string
    }
}

interface schoolName {
    id: string;
    name: string;
    address: string;
    
}
